import Page from "@/components/Page";
import OncoclinicasAppScreen from "@/templates/OncoclinicasAppScreen";

export default function AppOncoclinicas(): JSX.Element {
  return (
    <Page title="App Oncoclínicas por você">
      <OncoclinicasAppScreen />
    </Page>
  );
}
