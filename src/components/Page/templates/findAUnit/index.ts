import { FindAUnitsRoot } from "./root";
import { SingleUnitRoot } from "./SingleUnit";

export const findAUnitsTemplates = {
  UNITS: FindAUnitsRoot,
};

export const singleUnitTemplates = {
  UNIT: SingleUnitRoot,
};
