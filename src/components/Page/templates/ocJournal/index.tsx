import { OCJournalRoot } from "./root";
import { SingleJournalRoot } from "./SingleJournal";

export const ocJournalTemplates = {
  OC_JOURNAL: OCJournalRoot,
};

export const singleJournalTemplates = {
  JOURNAL_ARTICLE: SingleJournalRoot,
};
